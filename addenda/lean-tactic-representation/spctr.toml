version = 1
license = "Mixed: PolyForm-Noncommercial-1.0.0 (code), CC-BY-NC-4.0 (docs)"
title = "lean-tactic-representation"
series = "A-004"
summary = "Algebraic, round-trippable representations of Lean proof-state tactics."
status = "concept"

[site]
visible = true
featured = false
publish_docs = false

[labels]
type = "research"

[release]
stage = "candidate"

[[release.surfaces]]
name = "source"
kind = "source_bundle"
publish = true

[[release.surfaces]]
name = "python"
kind = "package"
publish = false
language = "python"
registry = "pypi"
publish_mode = "subdir"

[spctr]
project = "lean-tactic-representation"

[spctr.exec.check]
description = "Canonical candidate verification lane"
commands = [
    ["lake", "build"],
    ["uv", "run", "ruff", "check", "."],
    ["uv", "run", "ty", "check", "."],
    ["uv", "run", "python", "-m", "pytest"],
]
timeout_sec = 1800
network = "off"
requires = ["python", "uv", "lean"]

[spctr.exec.smoke]
description = "Compile, execute, and kernel-check one structured program"
command = ["bash", "-lc", '''set -euo pipefail
mkdir -p artifacts/smoke
uv run python cli.py compile-run scenarios/source/constructor.json \
  > artifacts/smoke/compiler-receipt.json
''']
expected_outputs = ["compiler-receipt"]
timeout_sec = 600
network = "off"
requires = ["python", "uv", "lean"]

[spctr.runtime]
platforms = ["linux", "macos"]
requires = ["python", "uv", "lean"]
network = "off"
cache_paths = [".lake", ".venv", ".ruff_cache", ".pytest_cache"]

[spctr.ci]
runner = "ubuntu-latest"
pull_request = ["check"]
push_main = []
nightly = []

[spctr.evidence]
card_path = "artifacts/evidence/latest.json"

[[spctr.expected_outputs]]
name = "compiler-receipt"
path = "artifacts/smoke/compiler-receipt.json"
kind = "json"
required = true

[spctr.docs]
root = "docs"
landing = "README.md"
require_frontmatter = false
