[project]
name = "tt-lenia"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.1.0",
]

[project.optional-dependencies]
tt = []

[tool.uv]
dev-dependencies = [
    "pytest>=8.0",
]

[tool.ruff]
line-length = 100
