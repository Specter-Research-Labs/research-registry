# Generated by `spctr ci sync --write`; edit spctr.toml instead.

name: lean-sorry-repos-benchmark-ci

on:
  pull_request:
    paths:
      - 'addenda/lean-sorry-repos-benchmark/**'
  push:
    branches: [main]
    paths:
      - 'addenda/lean-sorry-repos-benchmark/**'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  pull_request:
    if: ${{ github.event_name == 'pull_request' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    timeout-minutes: 60
    steps:
      - uses: actions/checkout@v4
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
      - name: Cache cargo build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ops/spctr/target
          key: spctr-${{ runner.os }}-${{ hashFiles('ops/spctr/Cargo.lock') }}
          restore-keys: spctr-${{ runner.os }}-
      - name: Build spctr
        run: cargo build --release --locked --manifest-path ops/spctr/Cargo.toml
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Setup uv
        uses: astral-sh/setup-uv@v8.0.0
        with:
          version: '0.9.26'
          enable-cache: true
          working-directory: addenda/lean-sorry-repos-benchmark
          cache-dependency-glob: |
            pyproject.toml
            uv.lock
      - name: Sync Python dependencies
        working-directory: addenda/lean-sorry-repos-benchmark
        run: uv sync --frozen --dev
      - name: Run exec check
        run: ./ops/spctr/target/release/spctr exec run --project lean-sorry-repos-benchmark check
      - name: Run exec smoke
        run: ./ops/spctr/target/release/spctr exec run --project lean-sorry-repos-benchmark smoke
  push_main:
    if: ${{ (github.event_name == 'push' && github.ref == 'refs/heads/main') || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    timeout-minutes: 140
    steps:
      - uses: actions/checkout@v4
      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
      - name: Cache cargo build
        uses: actions/cache@v4
        with:
          path: |
            ~/.cargo/registry
            ~/.cargo/git
            ops/spctr/target
          key: spctr-${{ runner.os }}-${{ hashFiles('ops/spctr/Cargo.lock') }}
          restore-keys: spctr-${{ runner.os }}-
      - name: Build spctr
        run: cargo build --release --locked --manifest-path ops/spctr/Cargo.toml
      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.12'
      - name: Setup uv
        uses: astral-sh/setup-uv@v8.0.0
        with:
          version: '0.9.26'
          enable-cache: true
          working-directory: addenda/lean-sorry-repos-benchmark
          cache-dependency-glob: |
            pyproject.toml
            uv.lock
      - name: Sync Python dependencies
        working-directory: addenda/lean-sorry-repos-benchmark
        run: uv sync --frozen --dev
      - name: Verify declared runtime requirements
        run: |
          set -euo pipefail
          if ! command -v docker >/dev/null; then
            echo "missing required runtime: docker" >&2
            exit 1
          fi
          if ! command -v tar >/dev/null; then
            echo "missing required runtime: tar" >&2
            exit 1
          fi
      - name: Run exec check
        run: ./ops/spctr/target/release/spctr exec run --project lean-sorry-repos-benchmark check
      - name: Run exec smoke
        run: ./ops/spctr/target/release/spctr exec run --project lean-sorry-repos-benchmark smoke
      - name: Run exec build
        run: ./ops/spctr/target/release/spctr exec run --project lean-sorry-repos-benchmark build
      - name: Run exec publish
        run: ./ops/spctr/target/release/spctr exec run --project lean-sorry-repos-benchmark publish
      - name: Evaluate release gate
        run: ./ops/spctr/target/release/spctr release gate lean-sorry-repos-benchmark
