# Generated by `spctr ci sync --write`; edit spctr.toml instead.

name: lean-tactic-representation-ci

on:
  pull_request:
    paths:
      - 'addenda/lean-tactic-representation/**'
  workflow_dispatch:

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

permissions:
  contents: read

jobs:
  pull_request:
    if: ${{ github.event_name == 'pull_request' || github.event_name == 'workflow_dispatch' }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v6.0.2
      - name: Setup spctr project
        uses: ./.github/actions/setup-spctr
        with:
          project-root: 'addenda/lean-tactic-representation'
          use-python: 'true'
          python-version: '3.12'
          use-uv: 'true'
          sync-python: 'true'
          use-lean: 'true'
      - name: Run exec check
        run: ./ops/spctr/target/release/spctr exec run --project lean-tactic-representation check
